import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input:{
        width:"100%",
        height: 64,
        borderWidth: 2,
        borderRadius: 16,
        marginTop: 8,
        fontSize: 20,
        paddingLeft: 24
    }
})
